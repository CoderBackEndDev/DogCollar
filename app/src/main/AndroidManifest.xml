<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.admin.dogcollar">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.DEVICE_POWER"
        tools:ignore="ProtectedPermissions" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".loadout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".User_registration" />
        <activity android:name=".MainActivity" />
        <activity android:name=".editPetProfile" />
        <activity android:name=".Content" />
        <activity android:name=".ChangePass" />
        <activity android:name=".pet_Registration" />
        <activity android:name=".profile" />
        <activity android:name=".AntiBark" />
        <activity android:name=".Gym" />
        <activity android:name=".Infrared" />
        <activity android:name=".Location" />
        <activity android:name=".Notification" />
        <activity android:name=".RFID" />
        <activity android:name=".Schedule" />
        <activity android:name=".RecTasks" />
        <activity android:name=".Mood" />
        <activity android:name=".editUserProfile" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsLocation"
            android:label="@string/title_activity_maps_location" />
        <activity android:name=".Status" />
        <activity android:name=".PetSaveFbase" />
        <activity android:name=".UserSaveFbase"></activity>
    </application>

</manifest>